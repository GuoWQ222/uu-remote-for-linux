#!/usr/bin/env bash
set -euo pipefail

if [[ -e /proc/$$/fd/9 ]]; then
    printf 'FD9_LEAK wineserver %s\n' "$*" >>"${UU_REMOTE_FAKE_TRACE:?}"
fi

printf 'WINESERVER %s\n' "$*" >>"${UU_REMOTE_FAKE_TRACE:?}"
